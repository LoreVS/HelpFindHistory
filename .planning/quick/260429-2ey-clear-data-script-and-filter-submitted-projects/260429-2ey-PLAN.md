---
phase: quick
plan: 260429-2ey
type: execute
wave: 1
depends_on: []
files_modified:
  - server/scripts/clear-data.js
  - puzzle-fragments/src/pages/ProjectsPage.jsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running node server/scripts/clear-data.js deletes all rows from projects, fragments, and attempts but leaves users intact"
    - "On ProjectsPage, the main grid never shows a project for which the current user already has a published, approved, or rejected attempt"
    - "Projects with a finished attempt still appear in the My Finished Projects section"
  artifacts:
    - path: "server/scripts/clear-data.js"
      provides: "Dev-reset script — truncates projects, fragments, attempts"
    - path: "puzzle-fragments/src/pages/ProjectsPage.jsx"
      provides: "Filtered main grid excluding user's finished projects"
  key_links:
    - from: "puzzle-fragments/src/pages/ProjectsPage.jsx"
      to: "useProjectStore myAttempts"
      via: "Set of finished project IDs derived from myAttempts; projects.filter() uses that set"
---

<objective>
Two independent dev-quality-of-life changes:

1. A server-side reset script that wipes transient data (projects, fragments, attempts) without touching user accounts — useful for dev iteration.
2. A client-side filter on ProjectsPage so the main projects grid excludes projects the current user has already finished (published/approved/rejected attempt). Those projects are already shown in "My Finished Projects" and should not appear twice.

Purpose: Improve dev workflow (script) and eliminate duplicate project cards in the UI (filter).
Output: server/scripts/clear-data.js + updated ProjectsPage.jsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- Server uses better-sqlite3 (synchronous API). db.js resolves DB_PATH from env or default ./data/puzzle_forge.db. -->
<!-- Schema tables: users, projects, fragments, attempts, scores, token_denylist -->
<!-- Foreign keys: fragments.project_id → projects(id) CASCADE, attempts.project_id → projects(id) CASCADE -->
<!-- myAttempts in store: Array<{ id, project_id, status, submitted_at, project_name, project_status }> -->
<!-- finished statuses: 'published' | 'approved' | 'rejected' -->

<interfaces>
From server/db.js:
  module.exports = db  // better-sqlite3 Database instance, DB_PATH from env or './data/puzzle_forge.db'

From puzzle-fragments/src/store/useProjectStore.js:
  myAttempts: []  // Array<{ id, project_id, status, submitted_at, project_name, project_status }>
  // populated by fetchMyAttempts() → GET /api/attempts/me
  // status IN ('published', 'approved', 'rejected') — these are the finished ones

From puzzle-fragments/src/pages/ProjectsPage.jsx:
  const { projects, loading, error, fetchProjects, createProject, myAttempts, fetchMyAttempts } = useProjectStore()
  // role !== 'admin' → fetchMyAttempts() is called on mount
  // Main grid: projects.map(...)  ← needs to be filtered
  // Finished section: myAttempts.map(...)  ← no change needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server/scripts/clear-data.js dev-reset script</name>
  <files>server/scripts/clear-data.js</files>
  <action>
Create the file at server/scripts/clear-data.js (relative to the repo root at /Users/apple/Desktop/Vortex).

The script must:
- Require '../db' (path relative to the scripts/ directory → resolves to server/db.js)
- Use better-sqlite3's synchronous API (db.prepare().run() — no async/await)
- Delete in dependency order to satisfy foreign key constraints:
    1. DELETE FROM attempts
    2. DELETE FROM fragments
    3. DELETE FROM projects
- Do NOT touch: users, scores, token_denylist
- Print a confirmation line after each delete showing the number of rows affected (use result.changes)
- Print a final "Done." line
- Exit cleanly (process.exit(0))

Example output when run:
  Deleted 12 attempt(s).
  Deleted 34 fragment(s).
  Deleted 8 project(s).
  Done.

No CLI args needed. Script is run directly: node server/scripts/clear-data.js
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/Vortex && node server/scripts/clear-data.js 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>Script runs without error, prints deleted row counts for attempts/fragments/projects, exits 0. Users table is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Filter finished projects from ProjectsPage main grid</name>
  <files>puzzle-fragments/src/pages/ProjectsPage.jsx</files>
  <action>
In ProjectsPage.jsx, derive a Set of project IDs that the current user has already finished, then filter the main grid to exclude them. The "My Finished Projects" section is unchanged.

Changes are confined to the component body (no store changes needed — myAttempts already contains the right data).

Step 1 — Build the finished set (add after the useProjectStore destructure line):

  const finishedProjectIds = new Set(myAttempts.map((a) => a.project_id))

Step 2 — Filter the main grid render. Change:

  {projects.map((project) => (

to:

  {projects.filter((p) => !finishedProjectIds.has(p.id)).map((project) => (

The filter applies only to the main grid div (className="projects-grid" before the finished-section). The finished-section's myAttempts.map() is untouched.

Also update the "No projects yet" empty-state check so it tests the filtered list, not the raw list. Replace:

  {!loading && projects.length === 0 && (

with:

  {!loading && projects.filter((p) => !finishedProjectIds.has(p.id)).length === 0 && (

To avoid the duplicated filter expression, extract a variable near the top of the return (or just before the grid JSX):

  const visibleProjects = projects.filter((p) => !finishedProjectIds.has(p.id))

Then use visibleProjects.length === 0 for empty-state and visibleProjects.map(...) for the grid. finishedProjectIds computation stays as a const in the component body.

Note: For admin users, myAttempts is [] (fetchMyAttempts is not called), so finishedProjectIds will be an empty Set — the filter is a no-op for admins. No role guard needed.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/Vortex/puzzle-fragments && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>
- `finishedProjectIds` Set is derived from myAttempts.
- Main projects grid renders `visibleProjects` (projects excluding finished ones).
- Empty-state message checks `visibleProjects.length === 0`.
- My Finished Projects section is unchanged.
- `npm run lint` passes with no new errors.
  </done>
</task>

</tasks>

<verification>
1. Start fresh: run `node server/scripts/clear-data.js` — confirm row counts print and DB has no projects/fragments/attempts; users remain.
2. Re-seed or create a project and submit an attempt as a non-admin user.
3. Reload ProjectsPage — the submitted project must NOT appear in the main grid.
4. The same project MUST appear in "My Finished Projects".
5. Admin user sees all projects in main grid (no filtering side-effect).
</verification>

<success_criteria>
- node server/scripts/clear-data.js exits 0, prints per-table delete counts, leaves users intact.
- Non-admin users see no duplicate project cards across main grid and My Finished Projects.
- npm run lint in puzzle-fragments passes clean.
</success_criteria>

<output>
After completion, create .planning/quick/260429-2ey-clear-data-script-and-filter-submitted-projects/260429-2ey-SUMMARY.md
</output>
